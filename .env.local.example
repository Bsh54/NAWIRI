# Copy this file to ".env.local" and put your real key in it.
# NEVER commit .env.local — it stays only on your machine and on Vercel (as an env var).
# Get / regenerate a key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here
